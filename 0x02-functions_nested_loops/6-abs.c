#include "main.h"
#include <stdlib.h>

/**
 * _abs - It will computes the absolute value of an integer.
 *
 * @n: The integer to be converted;
 *
 * Return: Returns an absolute/non-negative integer.
 */
int _abs(int n)
{
	return (abs(n));
}
